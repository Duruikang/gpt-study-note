下面是 **冒泡排序、快速排序、插入排序、选择排序** （升序）的时间复杂度总结（面试高频表）👇

---

# 一、时间复杂度总览

| 排序算法            | 最好时间复杂度    | 平均时间复杂度    | 最坏时间复杂度 | 是否稳定  |
| --------------- | ---------- | ---------- | ------- | ----- |
| 冒泡排序（Bubble）    | O(n)       | O(n²)      | O(n²)   | ✔ 稳定  |
| 快速排序（Quick）     | O(n log n) | O(n log n) | O(n²)   | ❌ 不稳定 |
| 插入排序（Insertion） | O(n)       | O(n²)      | O(n²)   | ✔ 稳定  |
| 选择排序（Selection） | O(n²)      | O(n²)      | O(n²)   | ❌ 不稳定 |

---

# 二、冒泡排序（Bubble Sort）

![Image](https://miro.medium.com/0%2A3GYw87Ix_9BOQXn6.png)

![Image](https://www.programiz.com/sites/tutorial2program/files/Bubble-sort-0.png)

思想：

相邻元素比较，大的往后交换。

过程：

```text
5 3 8 6
↓
3 5 6 8
```

时间复杂度：

* 最好：O(n)（已经有序）
* 最坏：O(n²)

特点：

* 实现简单
* 性能较差

代码：

```java
public static void bubbleSort(int[] arr){

    int n = arr.length;

    for(int i = 0; i < n-1; i++){

        for(int j = 0; j < n-1-i; j++){

            if(arr[j] > arr[j+1]){

                int temp = arr[j];
                arr[j] = arr[j+1];
                arr[j+1] = temp;

            }
        }
    }
}
```
---

# 三、快速排序（Quick Sort）

![Image](https://miro.medium.com/1%2AbIJvejLlEshkf-ehYJJlWQ.png)

![Image](https://builtin.com/sites/www.builtin.com/files/styles/ckeditor_optimize/public/inline-images/1_quicksort.jpg)

思想：

选一个基准值（pivot），把数组分成：

```text
左边 < pivot
右边 > pivot
```

再递归排序。

时间复杂度：

* 平均：O(n log n)
* 最坏：O(n²)（极端情况下）

特点：

* 实际中非常快
* 使用最广泛

代码：

```java
public static void main(String [] args) {
    int []arr = {9,5,6,0,7,-1};
    quickSort(arr, 0, arr.length - 1);
}
public static void quickSort(int[] arr, int left, int right){

    if(left >= right){
        return;
    }

    int pivot = arr[left];

    int i = left;
    int j = right;

    while(i < j){
        // 一定要先处理右侧的j指针，再处理左侧的i指针右移，
        // 因为我们一开始选定了标杆值为arr[left]，
        // 右侧指针先左移，保证最后i=j的位置的值一定是小于标杆值的，
        // 所以最后把i=j的值和标杆值互换保证遵守快排的基本准则：标杆值左侧元素都比标杆值小，右侧元素都比标杆值大。
        // 如果先动了左侧指针则无法保证，因为左侧指针在向右走时会找到一个比标杆值大的值，如果恰好处理完j出发了 i和j相遇，
        // 就会到最后一步，把标杆值和arr[i]互换，此时就把一个比标杆值大的值放到了左侧，这显然是违反定义的
        while(i < j && arr[j] >= pivot){
            j--;
        }

        while(i < j && arr[i] <= pivot){
            i++;
        }

        if(i < j){
            int temp = arr[i];
            arr[i] = arr[j];
            arr[j] = temp;
        }
    }

    arr[left] = arr[i];
    arr[i] = pivot;

    quickSort(arr, left, i-1);
    quickSort(arr, i+1, right);

}
```

---

# 四、插入排序（Insertion Sort）

![Image](https://miro.medium.com/1%2A_NOe6jL9veyR4yAyf85dzA.png)

![Image](https://visualgo.net/img/insertion_sort.png)

思想：

像整理扑克牌：

每次把一个元素插入到已排序部分。

示例：

```text
3 5 2 8

3 5
↓
2 插入前面
```

时间复杂度：

* 最好：O(n)
* 最坏：O(n²)

特点：

* 小数据效率高
* 基本有序时很快

代码：

```java
public static void insertionSort(int[] arr){

    int n = arr.length;

    for(int i = 1; i < n; i++){

        int key = arr[i];

        int j = i - 1;

        while(j >= 0 && arr[j] > key){

            arr[j+1] = arr[j];

            j--;

        }

        arr[j+1] = key;

    }
}
```

---

# 五、选择排序（Selection Sort）

![Image](https://www.programiz.com/sites/tutorial2program/files/Selection-sort-0.png)

![Image](https://he-s3.s3.amazonaws.com/media/uploads/2888f5b.png)

思想：

每次选择最小元素放到前面。

示例：

```text
4 2 7 1

第一轮：
找最小 1
```

时间复杂度：

* 始终 O(n²)

特点：

* 比较次数固定
* 不稳定

代码：

```java
public static void selectionSort(int[] arr){

    int n = arr.length;

    for(int i = 0; i < n-1; i++){

        int minIndex = i;

        for(int j = i+1; j < n; j++){

            if(arr[j] < arr[minIndex]){
                minIndex = j;
            }
        }

        int temp = arr[i];
        arr[i] = arr[minIndex];
        arr[minIndex] = temp;

    }
}
```

---

# 六、复杂度记忆口诀

```text
冒泡、插入、选择 → O(n²)

快速排序 → O(n log n)
```

或：

```text
3个平方 1个对数
```

---

# 七、面试常问点

### 哪个最快？

平均情况下：

```text
快速排序最快
```

---

### 哪个稳定？

稳定排序：

* 冒泡
* 插入

不稳定：

* 快速
* 选择

---

### 哪个适合小数据？

```text
插入排序
```

---

# 八、一句话总结

| 算法   | 核心复杂度      |
| ---- | ---------- |
| 冒泡排序 | O(n²)      |
| 快速排序 | O(n log n) |
| 插入排序 | O(n²)      |
| 选择排序 | O(n²)      |

---

如果你愿意，我可以整理：

* 十大排序算法复杂度表
* 各排序动图总结
* 面试常考排序题型
