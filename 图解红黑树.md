下面用**图解方式**带你理解 **红黑树（Red-Black Tree）**：它是最常用的**自平衡二叉搜索树**，在 Java 中应用非常广泛（如 `TreeMap`、`TreeSet`、`HashMap` 链表过长时转红黑树）。

---

# 一、红黑树是什么

红黑树 = **二叉搜索树（BST） + 颜色规则 + 自动平衡**

特点：

* 每个节点都有颜色：**红** 或 **黑**
* 通过颜色约束，使树保持大致平衡
* 查找效率稳定：

```text
O(log n)
```

---

# 二、红黑树结构示意

![Image](https://upload.wikimedia.org/wikipedia/commons/6/66/Red-black_tree_example.svg)

![Image](https://www.sahinarslan.tech/static/b9197983f36b5fc9e8c79a01af9eaf50/b36d0/red-black-tree-properties.jpg)

示例：

```text
            10(B)
           /     \
        5(R)     20(R)
       /   \     /   \
    3(B) 7(B) 15(B) 25(B)
```

---

# 三、红黑树 5 条性质（核心）

## 1️⃣ 每个节点是红色或黑色

```text
节点颜色只有两种：
红 或 黑
```

---

## 2️⃣ 根节点必须是黑色

```text
Root = Black
```

保证树的稳定结构。

---

## 3️⃣ 所有叶子节点（空节点 NIL）是黑色

叶子节点指的是：

```text
null节点
```

例如：

```text
3(B)
 / \
NIL NIL
```

---

## 4️⃣ 红色节点不能连续

```text
不能出现：

红 → 红
```

即：

红节点的子节点必须是黑色。

示例（错误）：

```text
   10(B)
   /
  5(R)
  /
 3(R) ❌
```

---

## 5️⃣ 从任意节点到叶子节点路径上的黑节点数量相同

示例：

```text
        10(B)
       /     \
    5(R)     20(R)
   /   \     /   \
 3(B) 7(B) 15(B) 25(B)
```

路径：

```text
10 → 5 → 3 → NIL   黑节点数量 = 3
10 → 20 → 25 → NIL 黑节点数量 = 3
```

必须相等。

这叫：

```text
黑高相同
```

---

# 四、为什么红黑树能保持平衡？

红黑树并不像 AVL 那样要求：

```text
高度差 ≤ 1
```

而是保证：

```text
最长路径 ≤ 最短路径 × 2
```

因此不会退化成链表：

```text
1
 \
  2
   \
    3
```

仍然保持较好性能。

---

# 五、插入节点示意

插入新节点时：

```text
新节点默认是红色
```

原因：

如果插入黑色，会破坏黑节点数量平衡。

示例：

插入 8：

```text
插入前：

      10(B)
     /
   5(R)

插入后：

      10(B)
     /
   5(R)
     \
      8(R)
```

出现：

```text
红红冲突
```

需要调整。

---

# 六、红黑树调整方式

红黑树调整主要依赖：

```text
旋转 + 变色
```

---

## 情况1：变色

![Image](https://pages.cs.wisc.edu/~jey/RBTInsertion.png)

![Image](https://pages.cs.wisc.edu/~tomer/RBTInsertionIMG.jpg)

当：

```text
父节点 = 红
叔节点 = 红
```

处理：

```text
父节点 → 黑
叔节点 → 黑
祖父节点 → 红
```

继续向上检查。

---

## 情况2：旋转

当：

```text
父节点 = 红
叔节点 = 黑
```

需要旋转：

类似 AVL 的：

```text
左旋
右旋
```

例如：

### 左旋

```text
    10
      \
      20
        \
        30
```

左旋后：

```text
      20
     /  \
   10    30
```

---

# 七、红黑树 vs AVL 树

| 对比   | AVL  | 红黑树  |
| ---- | ---- | ---- |
| 平衡程度 | 严格平衡 | 相对平衡 |
| 查询速度 | 更快一点 | 稍慢   |
| 插入效率 | 较慢   | 更快   |
| 旋转次数 | 多    | 少    |
| 工程应用 | 较少   | 非常多  |

工程中更常用：

```text
红黑树
```

因为：

插入删除性能更好。

---

# 八、Java 中的红黑树

使用红黑树的类：

```text
TreeMap
TreeSet
HashMap（链表过长时）
```

HashMap 规则：

```text
链表长度 > 8
转为红黑树
```

优化查询效率：

```text
O(n) → O(log n)
```

---

# 九、一句话总结

> 红黑树是一种带颜色规则的二叉搜索树，通过限制红黑节点分布，使树保持近似平衡，从而保证查找效率为 O(log n)。

---

如果你愿意，我可以继续帮你整理：

* 红黑树插入全过程图解
* AVL vs 红黑树完整对比图
* 二叉树结构总图（面试速记版）
